# 手机端适配测试指南

## 快速开始

### 1. 启动开发服务器

```bash
cd frontend
npm run dev
```

访问：http://localhost:3000

### 2. 浏览器测试（推荐）

#### Chrome DevTools 模拟器模式

1. 打开 Chrome DevTools (`F12` 或 `Ctrl+Shift+I`)
2. 点击"Responsive Design Mode"或按 `Ctrl+Shift+M`
3. 选择预设设备：
   - **iPhone SE**（320px）
   - **iPhone 12**（390px）
   - **iPhone 14 Pro Max**（430px）
   - **iPad**（768px）
4. 在三个关键断点测试：
   - **< 768px**：移动端布局（底部导航）
   - **≥ 768px**：PC 端布局（侧边栏）

#### 调整窗口宽度手动测试

1. 将浏览器窗口拖至 320px 宽
2. 逐步拖大到 1024px
3. 观察布局在 768px 处的切换

---

## 详细验证清单

### ✅ Phase 1：基础框架验证

#### 登录/注册页面（无需认证）

**流程：**
1. 访问 http://localhost:3000/login
2. 在不同屏幕尺寸下测试

**移动端（< 768px）检查点：**
- [ ] EduBuddy Logo 显示在表单上方
- [ ] 左侧品牌区隐藏
- [ ] 邮箱和密码输入框全宽
- [ ] 登录按钮全宽且高度 ≥ 44px
- [ ] 没有水平滚动条
- [ ] 文字清晰可读

**PC 端（≥ 768px）检查点：**
- [ ] 左侧蓝色品牌区显示
- [ ] 右侧登录表单占屏幕一半
- [ ] 按钮和输入框合理尺寸

---

### ✅ Phase 2：页面功能验证

#### 首页（需登录）

**流程：**
1. 登录账号
2. 跳转到首页
3. 在移动端 (< 768px) 检查

**移动端检查点：**
- [ ] 底部导航栏显示 5 个菜单项（🏠 首页、🤖 AI、📝 笔记、📊 统计、👤 我的）
- [ ] 点击"我的"图标可进入个人资料
- [ ] 欢迎横幅竖排显示（文字在左，连续打卡或 Logo 在右）
- [ ] 统计卡片单列排列（学习时长、错题待复习、任务完成）
- [ ] 快捷入口卡片 2 列排列
- [ ] 没有水平滚动条

**PC 端（≥ 768px）检查点：**
- [ ] 左侧侧边栏显示，宽度 256px
- [ ] 侧边栏包含所有菜单项
- [ ] 底部导航隐藏
- [ ] 顶部栏显示完整信息（待复习数字、连续打卡天数）
- [ ] 统计卡片 3 列排列
- [ ] 主内容区和快捷入口并排显示

---

#### 笔记列表页面

**流程：**
1. 从首页点击"笔记"菜单
2. 进入笔记列表页

**移动端（< 768px）检查点：**
- [ ] 学科过滤下拉菜单显示
- [ ] "新建笔记"按钮全宽显示
- [ ] 笔记卡片单列排列
- [ ] 每张卡片显示标题、摘要、日期
- [ ] 点击卡片可进入编辑页面

**PC 端（≥ 768px）检查点：**
- [ ] 笔记卡片 3 列排列
- [ ] 卡片间距合理
- [ ] 过滤栏和按钮在上方

---

#### AI 对话页面

**流程：**
1. 从首页点击"AI"菜单
2. 进入 AI 对话页面

**移动端（< 768px）检查点：**
- [ ] 左侧会话列表隐藏
- [ ] 顶部显示"📋 对话列表"按钮
- [ ] 学科选择下拉菜单显示
- [ ] 聊天消息区域占满屏幕
- [ ] 用户消息气泡宽度合理（不超过屏幕宽度）
- [ ] AI 头像隐藏，只显示消息内容
- [ ] 输入框底部固定，不被键盘遮挡
- [ ] 发送按钮可点击（≥ 44px）

**PC 端（≥ 768px）检查点：**
- [ ] 左侧会话列表显示，宽度 256px
- [ ] 学科过滤标签显示
- [ ] 会话列表显示历史对话
- [ ] 顶部"对话列表"按钮隐藏
- [ ] AI 头像显示在消息左侧

---

### 🔧 响应式断点测试

#### 768px 处的布局切换测试

**测试方法：**
1. 在 DevTools 中开启 Responsive Design Mode
2. 将窗口宽度设置在 765px～771px 之间缓慢调整
3. 观察以下元素的切换：

| 元素 | < 768px | ≥ 768px |
|------|---------|---------|
| 侧边栏 | 隐藏 | 显示 |
| 底部导航 | 显示 | 隐藏 |
| 顶部栏信息 | 简化（图标）| 完整（文字） |
| 卡片网格 | 1 列 | 2-3 列 |

**检查点：**
- [ ] 切换流畅，没有闪烁
- [ ] 布局在切换时正确调整
- [ ] 没有元素重叠或错位
- [ ] 文字和按钮尺寸适当调整

---

### 📱 真实设备测试

#### 准备
1. 打开开发服务器（运行 `npm run dev`）
2. 查看本机 IP（通常为 `192.168.x.x` 或 `10.0.x.x`）
3. 在手机浏览器访问：`http://<你的IP>:3000`

#### 测试要点
- [ ] 页面加载速度（< 3 秒）
- [ ] 没有 JavaScript 错误（检查 DevTools Console）
- [ ] 交互响应流畅
- [ ] 键盘弹出时表单仍可用
- [ ] 长按可复制文本
- [ ] 滚动流畅

#### 浏览器兼容性
- [ ] 测试 Chrome（Android）
- [ ] 测试 Safari（iOS）

---

## 常见问题排查

### 问题 1：移动端显示侧边栏而不是底部导航

**原因：** 断点设置错误或浏览器宽度 ≥ 768px

**解决：**
1. 检查浏览器窗口宽度（DevTools 左上角显示）
2. 确保宽度 < 768px
3. 刷新页面

### 问题 2：输入框被键盘遮挡

**原因：** 移动端键盘弹出导致视口缩小

**预期行为：**
- 页面应该滚动，使输入框保持可见
- 不应该发生，因为我们已添加 `pb-20` 预留空间

**调试：**
- 打开真实设备的 DevTools（iOS 需要 Mac + Safari，Android 需要 Chrome Remote Debugging）
- 查看是否有错误信息

### 问题 3：卡片布局错乱

**原因：** Tailwind CSS 未正确编译或缓存问题

**解决：**
```bash
npm run build
# 或
npm run dev
# 然后清空浏览器缓存：Ctrl+Shift+Delete
```

### 问题 4：底部导航在 PC 端仍显示

**原因：** CSS 响应式选择器 `.md:hidden` 未生效

**检查：**
- 在浏览器 DevTools 中检查元素
- 确认 `md:hidden` 类已应用
- 检查是否有其他 CSS 覆盖

---

## 性能测试

### Lighthouse 审计

1. 打开 Chrome DevTools
2. 切换到"Lighthouse"标签
3. 选择"Mobile"
4. 点击"分析网页加载情况"

**目标指标：**
- **LCP (Largest Contentful Paint)：** < 2.5 秒
- **CLS (Cumulative Layout Shift)：** < 0.1
- **FID (First Input Delay)：** < 100 毫秒（未来改为 INP）

### 手动性能检查

1. **开发者工具 → Performance 标签**
   - 点击"录制"
   - 刷新页面
   - 停止录制
   - 查看关键指标

2. **Network 标签检查加载时间**
   - 初始加载应 < 3 秒
   - 大型资源（图表库）应按需加载

---

## 测试报告模板

在完成测试后，创建报告（保存为 `MOBILE_TEST_REPORT.md`）：

```markdown
# 手机端适配测试报告

**测试日期：** YYYY-MM-DD  
**测试设备：** [iPhone 12 / Android 12 等]  
**浏览器：** [Chrome / Safari 等]  

## 整体状态

- [x] 通过 / [ ] 需修复

## 详细结果

### 导航和布局
- [x] 底部导航显示正常
- [x] PC 端侧边栏显示正常
- [ ] ...

### 功能
- [x] 登录/注册可用
- [x] 首页显示正常
- [ ] ...

### 性能
- LCP: X.X 秒（目标 < 2.5s）
- CLS: X.XX（目标 < 0.1）

## 发现的问题

1. **[优先级] 问题描述**
   - 复现步骤：...
   - 预期行为：...
   - 实际行为：...

## 建议

- ...
```

---

## 快速命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 类型检查
npm run type-check

# 获取本机 IP（Linux/Mac）
hostname -I
# 或
ifconfig

# 获取本机 IP（Windows）
ipconfig
```

---

**需要帮助？** 查看 `MOBILE_ADAPTATION_STATUS.md` 了解完整进度。
